import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lwj8k3bb-5000.inc1.devtunnels.ms','cgt-mvp-bucket.s3.amazonaws.com'],
  },
};

export default nextConfig;
